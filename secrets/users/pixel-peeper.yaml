# Example SOPS-encrypted secrets for pixel-peeper user
# This file should be encrypted before committing to git
#
# To encrypt this file:
#   sops -e -i secrets/users/pixel-peeper.yaml
#
# To decrypt and edit:
#   sops secrets/users/pixel-peeper.yaml

# Example user secrets (remove and replace with your actual secrets):
github_token: "replace-with-your-token"
api_key: "replace-with-your-key"

